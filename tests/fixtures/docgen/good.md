# Good Documentation Fixture

This file references valid repository paths.

See `src/main.cpp` for the entry point and `include/cli.hpp` for CLI parsing.

The build script lives at `build.sh`.

```bash
echo "hello world"
ls -la
```
