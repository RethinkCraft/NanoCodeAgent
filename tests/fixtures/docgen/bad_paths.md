# Bad Paths Fixture

This file references non-existent repository paths for testing.

The core logic is in `src/does_not_exist.cpp`.

The automation script is `scripts/docgen/fake_script.py`.

A valid path: `src/main.cpp`.
