# Bare Code Mentions Fixture

This file mentions code filenames that are not repo-relative path references.

The helper script name is `repo_map.py`.

The implementation file is `agent_loop.cpp`.

An actual repo-relative path: `scripts/docgen/repo_map.py`.
